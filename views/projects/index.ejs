<div class="container py-3">
    <table class="table table-bordered table-striped">
        <thead>
            <td>#</td>
            <td>Slug</td>
            <td>Name</td>
            <td>Created</td>
            <td>
                <div class="d-flex justify-content-end">
                    <a href="/projects/new" class="btn btn-primary ms-3">New</a>
                </div>
            </td>
        </thead>
        <tbody>
            <% if(contents.length > 0) { contents.map((el) => { %>
            <tr>
                <td><p><%= el.id %></p></td>
                <td><p><%= el.slug %></p></td>
                <td><p><%= el.name %></p></td>
                <td><p><%= new Date(el.timestamp).toLocaleString() %></p></td>
                <td>
                    <div class="d-flex justify-content-end">
                        <a href="/projects/edit?id=<%= el.id %>" class="btn btn-secondary">Edit</a>
                        <button class="btn btn-danger ms-3" onclick="delItem('<%= el.id %>')">Delete</button>
                    </div>
                </td>
            </tr>
            <% }) } else { %>
            <tr>
                <td colspan="6">
                    <p style="text-align: center">No Projects yet</p>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>
</div>

<script>
    function delItem(id) {
        const confirmed = confirm("Do you really want to delete this object?");

        if (confirmed) {
            window.location.href = "/projects/delete?id=" + id;
        }
    }
</script>
